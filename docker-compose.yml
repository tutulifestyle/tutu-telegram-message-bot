version: '3.8'

services:
  bot:
    build: .
    container_name: message_tutu_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/bot.db
